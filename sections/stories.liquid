<section class="stories" data-section-id="{{ section.id }}">
  <div class="container">
    <!-- Section Header -->
    <div class="stories__header">
      <div class="section-divider">
        <div class="section-divider__line section-divider__line--short"></div>
        <span class="section-divider__text">{{ section.settings.section_number | default: '02' }}</span>
        <div class="section-divider__line section-divider__line--long"></div>
      </div>
      
      <h2 class="stories__title">
        {{ section.settings.heading | default: 'STORIES' }}
      </h2>
      
      <div class="section-divider">
        <div class="section-divider__line section-divider__line--long"></div>
        <span class="section-divider__text">{{ section.blocks.size }}</span>
        <div class="section-divider__line section-divider__line--short"></div>
      </div>
    </div>

    <!-- Stories -->
    <div class="stories__list">
      {%- for block in section.blocks -%}
        <article class="story {% if forloop.index is even %}story--reverse{% endif %}" {{ block.shopify_attributes }}>
          <div class="story__grid">
            <!-- Image Section -->
            <div class="story__media">
              {%- if block.settings.image != blank -%}
                <img 
                  src="{{ block.settings.image | image_url: width: 800 }}"
                  alt="{{ block.settings.image.alt | escape | default: block.settings.title }}"
                  loading="{% if forloop.first %}eager{% else %}lazy{% endif %}"
                  class="story__image"
                  width="800"
                  height="600"
                >
              {%- else -%}
                <div class="story__placeholder"></div>
              {%- endif -%}
              
              <div class="story__badge">
                <span>{{ block.settings.type | default: 'ARTICLE' }}</span>
              </div>
            </div>
            
            <!-- Content Section -->
            <div class="story__content">
              <div class="story__meta">
                <span>{{ block.settings.date | default: '2026' }}</span>
              </div>
              
              <h3 class="story__heading">
                {{ block.settings.title | default: 'Story Title' }}
              </h3>
              
              <p class="story__excerpt">
                {{ block.settings.excerpt | default: 'Brief description of the story goes here.' }}
              </p>
              
              {%- if block.settings.link != blank -%}
                <a href="{{ block.settings.link }}" class="story__link">
                  <span>{{ block.settings.link_text | default: 'Learn More' }}</span>
                  <span class="story__link-arrow">→</span>
                </a>
              {%- endif -%}
            </div>
          </div>
        </article>
      {%- endfor -%}
    </div>
  </div>
</section>

{% style %}
  .stories {
    padding: 4rem 0;
    background: #fafafa;
    border-top: 1px solid rgba(0, 0, 0, 0.05);
  }

  .stories__header {
    margin-bottom: 3rem;
  }

  .stories__title {
    text-align: center;
    font-size: 1.5rem;
    font-family: 'Space Grotesk', monospace;
    font-weight: 300;
    color: #000;
    letter-spacing: 0.1em;
    line-height: 1;
    padding: 1.5rem 0;
  }

  @media (min-width: 768px) {
    .stories__title {
      font-size: 1.75rem;
    }
  }

  .stories__list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .story__grid {
    display: grid;
    grid-template-columns: 1fr;
    border: 1px solid rgba(0, 0, 0, 0.1);
    background: #fff;
    transition: border-color 0.3s ease;
  }

  .story__grid:hover {
    border-color: rgba(0, 0, 0, 0.3);
  }

  @media (min-width: 768px) {
    .story__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .story__media {
    position: relative;
    aspect-ratio: 16 / 10;
    overflow: hidden;
    background: #e5e7eb;
  }

  @media (min-width: 768px) {
    .story__media {
      aspect-ratio: 4 / 3;
    }

    .story--reverse .story__media {
      order: 2;
    }
  }

  .story__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.7s ease;
  }

  .story:hover .story__image {
    transform: scale(1.05);
  }

  .story__placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #e5e7eb 0%, #d1d5db 100%);
  }

  .story__badge {
    position: absolute;
    top: 1rem;
    left: 1rem;
    padding: 0.375rem 0.75rem;
    background: #000;
    color: #fff;
  }

  .story__badge span {
    font-size: 0.625rem;
    font-family: 'Space Grotesk', monospace;
    font-weight: 300;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  .story__content {
    padding: 2rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    border-top: 1px solid rgba(0, 0, 0, 0.1);
  }

  @media (min-width: 768px) {
    .story__content {
      padding: 2.5rem;
      border-top: none;
      border-left: 1px solid rgba(0, 0, 0, 0.1);
    }

    .story--reverse .story__content {
      order: 1;
      border-left: none;
      border-right: 1px solid rgba(0, 0, 0, 0.1);
    }
  }

  .story__meta {
    margin-bottom: 0.5rem;
  }

  .story__meta span {
    font-size: 0.625rem;
    font-family: 'Space Grotesk', monospace;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: rgba(0, 0, 0, 0.3);
  }

  .story__heading {
    font-size: 1.25rem;
    font-family: 'Space Grotesk', monospace;
    font-weight: 300;
    color: #000;
    letter-spacing: 0.05em;
    line-height: 1.2;
    margin-bottom: 0.75rem;
  }

  @media (min-width: 768px) {
    .story__heading {
      font-size: 1.5rem;
    }
  }

  .story__excerpt {
    font-size: 0.875rem;
    color: rgba(0, 0, 0, 0.5);
    line-height: 1.6;
    font-weight: 300;
    margin-bottom: 1.5rem;
  }

  .story__link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.75rem;
    font-family: 'Space Grotesk', monospace;
    color: rgba(0, 0, 0, 0.6);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .story__link:hover {
    color: #000;
  }

  .story__link span {
    letter-spacing: 0.05em;
  }

  .story__link-arrow {
    color: rgba(0, 0, 0, 0.2);
    transition: color 0.3s ease;
  }

  .story__link:hover .story__link-arrow {
    color: rgba(0, 0, 0, 0.4);
  }

  /* Section Divider */
  .section-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
  }

  .section-divider__line {
    height: 1px;
    background: rgba(0, 0, 0, 0.1);
  }

  .section-divider__line--short {
    width: 1.5rem;
  }

  .section-divider__line--long {
    width: 4rem;
  }

  @media (min-width: 768px) {
    .section-divider__line--short {
      width: 2rem;
    }

    .section-divider__line--long {
      width: 6rem;
    }
  }

  .section-divider__text {
    font-size: 0.6875rem;
    font-family: 'Space Grotesk', monospace;
    letter-spacing: 0.1em;
    color: rgba(0, 0, 0, 0.3);
  }
{% endstyle %}

{% schema %}
{
  "name": "Stories",
  "tag": "section",
  "class": "section-stories",
  "settings": [
    {
      "type": "text",
      "id": "section_number",
      "label": "Section Number",
      "default": "02"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "STORIES"
    }
  ],
  "blocks": [
    {
      "type": "story",
      "name": "Story",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "About Us"
        },
        {
          "type": "textarea",
          "id": "excerpt",
          "label": "Excerpt",
          "default": "The story of VAIKO STORE creation and our philosophy."
        },
        {
          "type": "text",
          "id": "type",
          "label": "Badge Text",
          "default": "ABOUT"
        },
        {
          "type": "text",
          "id": "date",
          "label": "Date",
          "default": "2026"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "text",
          "id": "link_text",
          "label": "Link Text",
          "default": "Learn More"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Stories",
      "blocks": [
        {
          "type": "story",
          "settings": {
            "title": "About Us",
            "excerpt": "The story of VAIKO STORE creation and our philosophy.",
            "type": "ABOUT",
            "date": "2026"
          }
        },
        {
          "type": "story",
          "settings": {
            "title": "How We Work",
            "excerpt": "The process from product selection to delivery.",
            "type": "PROCESS",
            "date": "2026"
          }
        },
        {
          "type": "story",
          "settings": {
            "title": "Our Standards",
            "excerpt": "What makes our products special.",
            "type": "QUALITY",
            "date": "2026"
          }
        }
      ]
    }
  ]
}
{% endschema %}
